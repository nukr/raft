package raft

import "testing"

func TestSetState(t *testing.T) {
	// n := nodeState{1, sync.Mutex{}}
	// expected := RaftState{1}
	// n.setState(expected)

	// result := n.getState()
	// if result != expected {
	// 	t.Errorf("result not equal expected %s but got %s", expected, result)
	// }
}
